services:
  db:
    image: postgres:15
    container_name: printing-db-container
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123123
      POSTGRES_DB: printing_db
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: