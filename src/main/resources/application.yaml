spring:
  application:
    name: Printing

  datasource:
    url: jdbc:postgresql://localhost:5432/printing_db
    username: postgres
    password: "123123"

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8081
