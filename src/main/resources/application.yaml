server:
  port: 8081  # This fixes the "Port already in use" error

spring:
  application:
    name: printing

  datasource:
    url: jdbc:postgresql://ep-summer-pond-an2szf6l-pooler.c-6.us-east-1.aws.neon.tech:5432/neondb?sslmode=require
    username: neondb_owner
    password: npg_8YCOvDXybks2
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          lob:
            non_contextual_creation: true