server:
  port: 8081

spring:
  application:
    name: printing

  datasource:
    url: jdbc:postgresql://ep-summer-pond-an2szf6l-pooler.c-6.us-east-1.aws.neon.tech:5432/neondb?sslmode=require
    username: neondb_owner
    password: npg_8YCOvDXybks2
    driver-class-name: org.postgresql.Driver
    # Add these Hikari settings to keep the cloud connection alive
    hikari:
      keepalive-time: 30000
      max-lifetime: 600000
      idle-timeout: 60000
      connection-timeout: 20000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        # This helps with cloud database stability
        temp:
          use_jdbc_metadata_defaults: false 

  # Add this to allow larger image uploads
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB